# NodeJs Hello-World

- `script.js` - To run ->

```bash
abhis@Tinku MINGW64 ~/Desktop/NodeJs/hello-world (main)
$ node script.js
Hello from script.js file!
# Make use of node --help to know what all we can
# we can make use node --watch [file_name] to re-run the js file automatically as we make changes in the file instead of us going to terminal back & forth & exit when we are done.
$ node --watch script.js
Restarting 'script.js'
Hello from script.js file!
Hello from script2-watch.js file! # do ctrl+c to exit 
Completed running 'script.js'

# the above is node cli
```
